interface:
  display_name: "Context Compress Repo"
  short_description: "Build and validate the OpenCode compression plugin"
  default_prompt: "Use $work-on-context-compress to implement and validate a change in this repository."
